import '../../../librearies.dart';

class SendOtpFromScreen extends StatefulWidget {
  const SendOtpFromScreen({super.key});

  @override
  State<SendOtpFromScreen> createState() => _SendOtpFromScreenState();
}

class _SendOtpFromScreenState extends State<SendOtpFromScreen> {
  @override
  Widget build(BuildContext context) {
    return CommonScreen(
        isBottomShape: true,
        child: Column(
          children: [],
        ));
  }
}
